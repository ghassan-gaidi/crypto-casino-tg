{
  "installCommand": "npm install && cd app && npm install",
  "buildCommand": "cd app && npx vite build",
  "outputDirectory": "app/dist",
  "crons": [
    {
      "path": "/api/cron",
      "schedule": "0 0 * * *"
    }
  ],
  "rewrites": [
    { "source": "/api/(dice|coinflip|crash|mines|plinko|slots|roulette|limbo|jackpot|balance|leaderboard|history|refs|stats)", "destination": "/api/game" },
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
